# run7_mcs6 phase 1: Matte-CNN only, hard gate on.
# zero_raw_matte disables Raw Matte while Matte-CNN remains enabled.

base: configs/run7_phase1.yaml

model:
  zero_raw_matte: true

training:
  schedule: all
  gate_alpha: 1.0
  resume: null

checkpointing:
  output_dir: checkpoints/run7_mcs6_phase1/

wandb:
  tags: [run7, run7_mcs6, phase1, matte-cnn-only, hard-gate]
